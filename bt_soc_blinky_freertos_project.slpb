---
parameters:
- name: "build_dir"
constants:
- name: "project_name"
  value: "bt_soc_blinky_freertos_project"
steps:
- task: "create_gbl"
  output: "{{build_dir}}/{{project_name}}.gbl"
  app: "{{build_dir}}/{{project_name}}.out"
